import { VerticalBox, GroupBox, HorizontalBox } from "std-widgets.slint";
import { MorphicBackground, UserFeedback } from "../components/index.slint";

export component WelcomeUserView inherits Window {
    MorphicBackground {
        preferred-width: parent.width;
        preferred-height: parent.height;
    }

    UserFeedback {
        icon: @image-url("../../assets/images/eeg.png");
        message: "Turn on the EEG and put it on your head";
    }
}
